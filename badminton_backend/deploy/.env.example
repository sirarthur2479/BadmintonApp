# Copy to .env beside docker-compose.yml, then fill in a real secret:
#   python -c "import secrets; print(secrets.token_hex(32))"
# Never commit the real .env.
JWT_SECRET=<paste-generated-secret-here>
JWT_EXPIRE_DAYS=30
