FROM python:3.12-slim

WORKDIR /srv/badminton-backend

COPY pyproject.toml ./
COPY app ./app

RUN pip install --no-cache-dir .

ENV DB_PATH=/data/badminton.db

EXPOSE 8000

CMD ["uvicorn", "app.main:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
