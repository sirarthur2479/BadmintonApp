# Updates

## 2026-07-06

| Task | Summary |
|---|---|
| TASK-021 | `badminton_backend/` scaffold: FastAPI app factory, env `Settings` (hard-fails without `JWT_SECRET` outside dev), SQLite schema mirroring current Flutter `toMap()` keys (JSON drills, Part-A columns, nullable intensity, FK cascades), register/login with PyJWT HS256 + pwdlib Argon2id (python-jose/passlib banned per research), `current_account` dependency + `/me` with full token-rejection matrix incl. deleted-account tokens. 16/16 pytest green |
| TASK-022 | Players CRUD under the account: list/create/update/delete with the shared `require_player` ownership dependency (cross-account access is a non-leaking 404), delete cascades sessions/tournaments/matches/custom tags via FK. 24/24 pytest green |
| TASK-023 | Player-scoped data API: sessions (verbatim current-Flutter payloads incl. JSON-array drills + Part-A fields, upsert/batch/any/update/delete), tournaments with nested matches (pipe scores, isWin 0/1, FK cascade, own-tournament guard), idempotent custom tags; 14-route parametrized cross-account-404 and auth-401 matrices. 64/64 pytest green |
| TASK-024 | Flutter auth (web-gated): `ApiClient` (bearer injection, typed `ApiException`, build-time `API_BASE_URL`), `AuthProvider` (login/register-auto-login/logout/restore, prefs-persisted JWT, readable errors), login/register screens, `AuthGate` in app.dart — non-web bypass test-locked so mobile stays offline-first; web skips demo seeding. 143/143 flutter tests green |
| TASK-025 | Player select (web flow): `Player` model + `PlayerProvider` (load, prefs-persisted active player restored across restarts, single-player auto-select, add with client UUID, clear-on-logout), `PlayerSelectScreen` (cards, add dialog, logout), `WebHome` routing after login, profile-tab switch-player action. 156/156 flutter tests green |

## 2026-07-05

| Task | Summary |
|---|---|
| TASK-001 | Data layer fixes: FK pragma on (B8), demo data seeds once behind prefs flag (B3), real pull-to-refresh (B7); headless sqflite_common_ffi test infra added (9 tests green) |
| TASK-002 | Profile screen fixes: FileImage avatar on device with web-safe conditional import (B1); form syncs from provider without clobbering edits (B2) |
| TASK-003 | Session photos persist to app documents dir with relative paths, delete with their session, and display as tap-to-view thumbnails on session cards (B4) |
| TASK-004 | Tournament delete added to UI, match delete confirms, shared confirmDelete helper (B6); streak math extracted to pure tested utils, Progress shows current + best streak (B5). Closes the B1-B8 bug batch |
| TASK-005 | Model round-trip test coverage added for `TrainingSession`, `Tournament`/`TournamentMatch`, and `PlayerProfile` (`test/models/`) — comma/pipe-join, nullable fields, and default fallback all characterized. 42/42 suite green, no production changes |
| TASK-006 | `sessionsPerWeek`/`avgIntensityPerWeek` take an injectable `now:` param for deterministic week-boundary/empty-week/averaging tests; removed the counter-app placeholder `widget_test.dart`. Closes the flutter-test-foundation use-case. 46/46 suite green |
| TASK-007 | Session model Part A foundation: drills stored as JSON array (comma-safe custom tags, legacy comma rows still readable), new `ReflectionData` module (6 fixed questions + tolerant JSON encode/decode), `TrainingSession` gains goal/reflection fields with backward-compatible defaults, `intensity` now nullable legacy. Fresh-install schema updated — v1 devices need TASK-008's migration. 60/60 suite green |
| TASK-008 | DB schema v2: `onUpgrade` rebuilds sessions table in a transaction (comma drills → JSON, intensity nullable, goal/reflection columns defaulted) — verified against a real v1 db file; `updateSession` UPDATE path and `custom_tags` table with idempotent CRUD, both with web in-memory branches. 68/68 suite green |
| TASK-009 | `SessionProvider.updateSession` (persists, re-sorts by date, notifies), custom-tag state (`customTags`/`allDrillTypes`, trim + blank/dup/built-in guards, loaded with `refresh()`), and `avgGoalAchievementPerWeek` via a shared `_avgPerWeek` helper that also makes `avgIntensityPerWeek` skip unrated sessions. 79/79 suite green |
| TASK-010 | Session editing end-to-end: `LogSessionScreen` edit mode (prefill incl. non-built-in drills, Update button, id preserved, photo re-copied only when changed with old file cleanup), `SessionCard` edit icon, history screen wiring. 87/87 suite green |
| TASK-011 | Log-session form redesign: goal field + 6-question reflection + `StarRating` goal-achievement score replacing the intensity slider (new sessions save null intensity, edits keep legacy), duration + notes demoted to collapsed Advanced, goal+drill validation, custom drill tag chips (add dialog, long-press delete for user tags only). 100/100 suite green |
| TASK-012 | Goal display everywhere: `AppTheme.goalScoreColor`, `SessionCard` goal line + colored star badge + player/coach remarks (legacy sessions keep intensity dots), `GoalAchievementChart` (8-week bars) + home last-session goal/stars and trend section (no more "Intensity null/5"), seed data enriched with goals/reflections. 111/111 suite green |
| TASK-013 | Markdown export: pure `ExportService` (`sessionToMarkdown` in the plan's ★-format incl. legacy sessions, `bulkExport` with inclusive range + chronological order + count header), `ExportScreen` (date range, live count, disabled-at-zero share button) reachable from history app bar, edit-mode share button (`share_plus`). Closes the log-session-part-a use-case. 124/124 suite green |
| TASK-014 | `badminton_track/` scaffold: hatchling src-layout package (heavy ML deps as opt-in extras so pytest runs headless), court geometry model in metres (corners, front-service intersections, near-half polygon), strict pydantic `TrackConfig` + checked-in `config.yaml` (qwen3:8b coach default), privacy gitignores. 9/9 pytest green |
| TASK-015 | Court calibration (Module 1): `build_homography` (exact 4-corner `getPerspectiveTransform`, collinear guard), feet-only projection invariant (`feet_point`/`project_feet` is the sole bbox path), `Calibration` JSON persistence, front-service-line reprojection sanity check (warn > 20 px), interactive corner-picker shell. 19/19 pytest green |
| TASK-016 | Footwork metrics core (pure, no I/O): telemetry contract, gap interpolation (<0.5 s filled, longer flagged as `Gap`, edges never extrapolated), NaN-tolerant excursion-episode state machine with recovery latency (peak→re-entry), `EpisodeStats` incl. latency trend slope, session aggregates. 34/34 pytest green |
| TASK-017 | Player tracking pipeline (Module 2): `iter_tracks` — the sole ultralytics seam (YOLO11n + ByteTrack, frame stride, extras guard) — plus pure `lock_target` (warmup near-half scoring, grace hysteresis, opponent can never fill in, NaN rows) and `run_footwork` CSV telemetry. 44/44 pytest green |
| TASK-018 | Stage 1 shippable: `courtmap.render_court_map` (to-scale court from the geometry model, time-coloured trace that breaks at NaN holes, base circle, episode peak markers) + `badminton-track` CLI (`calibrate`, `analyze --mode footwork` → CSV/PNG/NaN-safe summary JSON, exit-2 actionable errors, biomech placeholder). 54/54 pytest green |
| TASK-019 | Biomechanics (Module 3): triplet angle math + camera-side landmark tables, visibility-filtered angle series, zero-phase 6 Hz Butterworth (peak-position test proves no lag) with effective-fs from timestamps, oblique-view z-spread guard, `iter_pose_frames` PoseLandmarker seam (Tasks API only), `run_biomech` per-window summaries (raw min/max) + CLI `--mode biomech`. 69/69 pytest green |
| TASK-020 | Coach narrative (Module 4): deterministic `build_summary` (pre-worded facts, NaN can never leak), schema-constrained `CoachReport` (2–3 drills enforced), guarded Ollama chat (`-cloud` tags refused pre-network, ConnectionError/404 degrade to metrics-only), body-commentary lint + safety-note check with one named-violation retry, Flutter-style Markdown rendering, CLI `report` command. Closes the badmintontrack-12 use-case. 88/88 pytest + 124/124 Flutter green |
