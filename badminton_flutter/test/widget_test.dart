import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test', (tester) async {
    // End-to-end tests will be added once the app is running on device.
    expect(true, isTrue);
  });
}
